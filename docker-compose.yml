services:
  web:
    container_name: r4r
    build: .
    command: >
      sh -c "python manage.py migrate --noinput --settings=r4r.settings_$ENV &&
             npm ci &&
             trap 'kill 0' EXIT;
             python manage.py runserver --settings=r4r.settings_$ENV 0.0.0.0:$PORT &&
             npm run dev"
    volumes:
      - .:/src
    ports:
      - "$PORT:$PORT"
    env_file:
      - .env